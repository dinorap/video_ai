# VideoCreator - Build Nuitka + GitHub OTA

pip install -r requirements.txt

# Dev nhanh (giữ cache)
python build_fast_c++.py --dev

# Bản phát hành
python build_fast_c++.py --release --clean

# Upload GitHub Release (tạo VideoCreator.zip + update.json)
python build_fast_c++.py --release --clean --zip

# Output khách: dist\VideoCreator\  (giải nén → chạy VideoCreator.exe)
# Upload release: VideoCreator.zip + update.json (cùng tag version trong version.py)

# OTA: nút Cập nhật trong app → xcopy ghi đè (không cần update.exe)
