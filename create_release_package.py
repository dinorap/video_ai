"""
DEPRECATED — dùng build Nuitka + --zip thay thế:

  python build_fast_c++.py --release --clean --zip

Sẽ tạo VideoCreator.zip và update.json tại thư mục gốc project.
"""
import sys

if __name__ == "__main__":
    print(__doc__)
    sys.exit(0)
